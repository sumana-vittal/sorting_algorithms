#  two methods

1. Lomuto Partioning
2. Hoare's Partitioning

function helper(a, start, end):
    if start == end:
        return

    pivot = a[random.randint(0, len(a)-1)]
    swap a[pivot], a[start]

    small = start
    for big in range(start+1, end):
        if a[big] < a[start]:
            small ++
            swap a[big], a[small]
    swap a[start], a[small]

    helper(a, start, small)
    helper(a, small+1, end)

    return a

fun quick_sort(arr):
    sorted_arr = helper(arr, 0, len(arr)-1)
    return sorted_arr



